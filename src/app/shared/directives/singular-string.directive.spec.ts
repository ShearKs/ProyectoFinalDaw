import { SingularStringDirective } from './singular-string.directive';

describe('SingularStringDirective', () => {
  it('should create an instance', () => {
    const directive = new SingularStringDirective();
    expect(directive).toBeTruthy();
  });
});
